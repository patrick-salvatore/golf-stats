{
  "name": "golf-stats",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd client && pnpm install && cd ../server && mix deps.get",
    "dev:client": "cd client && pnpm run dev",
    "dev:server": "cd server && mix phx.server",
    "dev": "pnpm run dev:server & pnpm run dev:client"
  }
}
